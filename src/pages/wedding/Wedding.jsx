import React from 'react';
import './Wedding.scss';

export const Wedding = () => (
    <section>
        <div className="wedding">
            <span>{}</span>
        </div>
    </section>
);         
